[workspace]
resolver = "2"
members = [
    "zentiff",
    "zensvg",
    "zenjp2",
    "zenpdf",
]

[patch.crates-io]
# TEMPORARY: zencodec::estimate (unified resource-estimation API) is unpublished.
# Pin to the git rev until zencodec 0.1.24 publishes, then drop this patch and
# lower the `zentiff` zencodec req back to a published version.
zencodec = { git = "https://github.com/imazen/zencodec.git", rev = "0f71295" }
